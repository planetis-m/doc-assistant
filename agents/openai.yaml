interface:
  display_name: "Doc Assistant"
  short_description: "Store and search chunkvec documents with a fixed workspace DB"
  default_prompt: "Use $doc-assistant in store or search mode with the default workspace database, chunk and label content for chunkvec, and infer cvquery filters from natural-language intent when the cues are clear."
